---
layout: default
---
<h1>{{ page.name }}</h1>
<img src="{{ page.image }}" alt="{{ page.name }}" style="width:300px;">

<ul>
    <li><strong>Breed:</strong> {{ page.breed }}</li>
    <li><strong>Age:</strong> {{ page.age }}</li>
    <li><strong>Personality:</strong> {{ page.personality }}</li>
</ul>

<div class="bio">
    {{ content }}
</div>

<a href="/">Back to all kittens</a>
